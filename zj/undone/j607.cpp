#include <bits/stdc++.h>
#define ouo ios_base::sync_with_stdio(false), cin.tie(0)
#define ll long long
using namespace std;

string str;
int idx;
stack<char> sk;
ll ans = 0;

int func() {
    if (str[idx] == 'f') {
        
    }
}

int main() {
    ouo;
    cin >> str;
    // for (int i = 0; i < str.length(); i++) {
    //     if (str[i] == 'f') {

    //     }
    // }

    func();
    cout << ans;
    return 0;
}

#include <bits/stdc++.h>
#define ouo ios_base::sync_with_stdio(false), cin.tie(0)
#define ll long long
#define db double
using namespace std;

int p, q, r, m;

int main() {
    cin >> p >> q >> r >> m;

    int in[p];
    for (int i = 0; i < p; i++) cin >> in[i];

    int logic[q];
    for (int i = 0; i < q; i++) cin >> logic[i];



    return 0;
}

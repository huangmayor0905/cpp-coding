#include <bits/stdc++.h>
#define ouo ios_base::sync_with_stdio(false), cin.tie(0)
#define ll long long
#define db double
using namespace std;

int t, s, p;
int c[5] = {100, 80, 60, 40, 20};

int main() {
    cin >> t >> s >> p;
    while (t >= t / 5) {
        if ()
    }
    return 0;
}

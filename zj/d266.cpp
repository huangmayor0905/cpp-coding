#include <cmath>
#include <iostream>
using namespace std;

int main() {
    // ios_base::sync_with_stdio(false);
    // cin.tie(0);

    int n;
    while (cin >> n) {
        int OUO[1000] = {0};
        OUO[0] = 1;
        for (int i = 0; i < n; i++) {
            
        }
    }
    return 0;
}

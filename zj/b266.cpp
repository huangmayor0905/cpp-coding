#include <algorithm>
#include <iostream>
using namespace std;

int main() {
    int r, c, m;
    cin >> r >> c >> m;

    int arr[r][c];
    for (int i = 0; i < r; i++) {
        for (int j = 0; j < c; j++) {
            cin >> arr[i][c];
        }
    }

    int q[m];
    for (int i = 0; i < m; i++) {
        cin >> q[i];
    }

    for (int i = m - 1; i >= 0; i--) {
        if ()
    }
    return 0;
}

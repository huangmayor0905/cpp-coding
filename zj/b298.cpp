#include <iostream>
#include <queue>
#include <vector>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    
    int n, m, l, q;
    cin >> n >> m >> l >> q;
    vector<vector<int>> ouo;
    int temp_a, temp_b;
    for (int i = 0; i < m; i++) {
        
    }

    return 0;
}
